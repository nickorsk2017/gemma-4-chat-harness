# EXEC — 2026-07-05-env-orchestrator-db-url
## v1
- .env: normalized ORCHESTRATOR_DATABASE_URL (removed spaces around '='), added
  POSTGRES_USER=postgres / POSTGRES_PASSWORD=250244 / POSTGRES_DB=gemma-chat +
  comments (docker vs local host note). Single file changed.
