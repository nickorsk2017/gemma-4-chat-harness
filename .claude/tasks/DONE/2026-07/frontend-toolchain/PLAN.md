# PLAN — frontend-toolchain

plan_version: 1

## Version pins (R2 — latest stable, verified July 2026)
- next ^16.2.10, react ^19.2.0, react-dom ^19.2.0, zustand ^5.0.14
- typescript ^5.9, @types/node ^22.10, @types/react ^19.2, @types/react-dom ^19.2
- tailwindcss ^4.3.2, @tailwindcss/postcss ^4.3.2, postcss ^8.5
- eslint ^9, eslint-config-next ^16.2.10, @eslint/eslintrc ^3.2
- jest ^30, jest-environment-jsdom ^30, @types/jest ^30
- @testing-library/react ^16.1, @testing-library/jest-dom ^6.6,
  @testing-library/dom ^10.4, @testing-library/user-event ^14.5

## File set (Executor writes) — project root = frontend/
- P1 package.json        scripts dev/build/start/lint/test(+watch); deps above (R2,A1)
- P2 tsconfig.json       strict; paths "@/*"->"./*"; next plugin; include next-env (R3,A2)
- P3 next-env.d.ts       Next ambient refs (regenerated by build)
- P4 next.config.ts      minimal typed config (R3)
- P5 postcss.config.mjs  { plugins: ["@tailwindcss/postcss"] } (Tailwind v4) (R3)
- P6 eslint.config.mjs   flat config via FlatCompat -> next/core-web-vitals + next/typescript (R3)
- P7 jest.config.mjs     next/jest, jsdom, setupFilesAfterEnv (R3,R5)
- P8 jest.setup.ts       import '@testing-library/jest-dom' (R5)
- P9 .gitignore          node_modules/.next/coverage/next-env.d.ts etc.
- P10 app/globals.css    @import "tailwindcss"; (Tailwind v4 wiring) (A3)
- P11 app/layout.tsx     root layout importing globals.css (A3)
- P12 app/page.tsx       Hello World heading (R4,A3)
- P13 __tests__/page.test.tsx  renders Page, asserts /hello world/i heading (R5,A4)

## Boundary decisions
- S1 Tests live in frontend/__tests__/ (NOT app/) to keep app = pages only.
- S2 Import alias @/app/page from test (P2 paths).
- S3 jest config is .mjs (JS) -> no ts-node needed; .ts setup transformed by next/jest SWC.
- S4 node_modules NOT produced here: sandbox is Linux+ARM w/ no npm registry (403);
     macOS binaries differ. Ship source+config; operator runs `pnpm install` (A5).

## Verification strategy (for Validator)
- Static: JSON parse package.json/tsconfig; sanity-check config contents + cross-refs.
- Runtime (build + `pnpm test`) is deferred to operator machine; document the commands.

## Risks
- Wrong platform binaries if installed here -> mitigated by S4 (don't install).
- Version drift -> caret ranges + operator lockfile capture latest patch.

## Out of scope
frontend/CLAUDE.md, Zustand store impl, real UI beyond Hello World, CI wiring.
