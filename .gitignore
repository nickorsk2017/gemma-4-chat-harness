.DS_Store

# Python
__pycache__/
*.py[cod]
.venv-gemma/
backend/.venv/
mcp/.venv/

# Docker-free local stack (`make dev`): pid files + service logs
.dev/

# Secrets — never commit real keys
.env
.env.*
!.env.example

# Chat attachments saved by the gateway (local dev)
uploads/

_to_delete