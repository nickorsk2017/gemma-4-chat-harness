# types/

**Role:** `*.d.ts` declarations for all entities.

Ambient/shared TypeScript types describing domain entities and cross-cutting
contracts used across `app`, `shared`, `services`, and `stores`.

- Allowed: `*.d.ts` files, interfaces/types for entities and API contracts.
- Forbidden: runtime code (no exported values/functions).
