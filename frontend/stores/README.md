# stores/

**Role:** Zustand stores.

Global/shared client state. Store definitions and selectors only.

- Allowed: `create` store definitions, selectors, store-level actions.
- Forbidden: HTTP calls (use `services/`), UI components.
